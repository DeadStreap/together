generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}


model content_items {
  id        Int      @id @default(autoincrement())
  title     String?
  content   String?
  createdAt DateTime @default(now())
}
